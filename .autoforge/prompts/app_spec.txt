<project_specification>
  <project_name>Home Designer</project_name>

  <overview>
    Home Designer is an open-source, self-hosted 3D interior design application that enables users to design and visualize their apartment or house interiors. Users can create rooms through multiple methods (drawing walls, inputting dimensions, uploading floor plans, or AI-powered photo reconstruction), furnish them with items from a built-in library or AI-generated 3D models, and customize every surface with materials and lighting. The app leverages Microsoft TRELLIS for image-to-3D model generation and provides a polished, reactive editing experience inspired by games like The Sims.
  </overview>

  <technology_stack>
    <frontend>
      <framework>React 18+ with TypeScript</framework>
      <3d_engine>Three.js via React Three Fiber (@react-three/fiber) and Drei (@react-three/drei)</3d_engine>
      <styling>Tailwind CSS with shadcn/ui components</styling>
      <state_management>Zustand (for editor state, camera, selections, undo/redo history)</state_management>
      <animations>Framer Motion (UI transitions and animations)</animations>
      <icons>Lucide React</icons>
      <build_tool>Vite</build_tool>
      <design_reference>21st.dev aesthetic - sleek, modern, minimal with purposeful animations</design_reference>
    </frontend>
    <backend>
      <runtime>Node.js 20+ with Express</runtime>
      <database>SQLite via better-sqlite3 (local storage, no external DB required)</database>
      <file_storage>Local filesystem for 3D models, textures, and project assets</file_storage>
      <ai_integration>Microsoft TRELLIS (image-to-3D), configurable AI API keys for photo-to-room reconstruction</ai_integration>
      <web_scraping>Cheerio + Puppeteer for product URL scraping (furniture specs and images)</web_scraping>
    </backend>
    <communication>
      <api>REST API with JSON payloads</api>
      <file_upload>Multipart form data for images and 3D models</file_upload>
      <realtime>None required (single-user local app)</realtime>
    </communication>
    <3d_pipeline>
      <model_formats>glTF/GLB (primary), OBJ (import support)</model_formats>
      <optimization>Draco compression for 3D models, LOD (Level of Detail) for performance</optimization>
      <textures>Compressed textures (KTX2/Basis Universal) for materials</textures>
    </3d_pipeline>
  </technology_stack>

  <prerequisites>
    <environment_setup>
      - Node.js 20+ and npm
      - Git
      - Modern browser with WebGL 2.0 support
      - (Optional) NVIDIA GPU for TRELLIS local inference, or TRELLIS API endpoint
      - User-provided API keys for AI services (configured in settings)
    </environment_setup>
  </prerequisites>

  <feature_count>125</feature_count>

  <security_and_access_control>
    <user_roles>
      <role name="user">
        <permissions>
          - Full access to all features (single-user application)
          - Create, read, update, delete all projects
          - Configure all settings including API keys
          - Import and export all data
        </permissions>
        <protected_routes>
          - None (no authentication required, fully local app)
        </protected_routes>
      </role>
    </user_roles>
    <authentication>
      <method>none - single-user local application</method>
      <session_timeout>none</session_timeout>
      <password_requirements>none</password_requirements>
    </authentication>
    <sensitive_operations>
      - API key storage should be encrypted at rest in the local database
      - Confirmation dialog before deleting projects
      - Confirmation dialog before deleting rooms with furniture
      - Warning before overwriting existing project on import
    </sensitive_operations>
  </security_and_access_control>

  <core_features>
    <infrastructure>
      - Database connection established and healthy
      - Database schema applied correctly with all tables
      - Data persists across server restart
      - No mock data patterns in codebase (no hardcoded arrays, no fakeData)
      - Backend API queries real SQLite database for all operations
    </infrastructure>

    <project_management>
      - Create new project with name and optional description
      - Open existing project from project list
      - Save project (manual save and auto-save)
      - Duplicate project (for exploring different design options for same apartment)
      - Delete project with confirmation dialog
      - Project list view with thumbnails, names, and last-modified dates
      - Recent projects quick access
      - Auto-save with configurable interval
    </project_management>

    <room_creation_and_editing>
      - Draw walls by dragging rectangles (Sims-style) with live dimension display
      - Dimensions auto-adjust as user drags to resize
      - Attach new rooms to existing rooms to form apartment structure
      - Create room by inputting exact dimensions (still adjustable after creation)
      - Upload floor plan image and convert to room structure
      - AI photo-to-room: upload apartment photo and AI reconstructs room layout with estimated dimensions
      - Resize rooms by dragging walls/corners
      - Reshape rooms (adjust form beyond simple rectangles - L-shapes, irregular rooms)
      - Delete room with furniture warning (options: delete furniture, keep furniture in space, or cancel)
      - Room naming and labeling
      - Display room dimensions (m2/sqft) on hover and in properties panel
      - Minimum room size validation
      - Multi-floor support: create multiple levels/floors per project
      - Floor switcher: vertical thumbnail stack showing all floors
      - Current floor renders at full opacity; other floors shown as translucent hints
      - Effortless floor switching with smooth transitions
      - Add/remove/reorder floors
      - Staircase/opening placement between floors
    </room_creation_and_editing>

    <3d_viewport_and_camera>
      - Default isometric view with orbit, pan, and zoom controls
      - First-person walkthrough mode with WASD/arrow key movement
      - Smooth animated transitions between camera modes
      - Camera position memory per floor
      - Day/night lighting toggle with smooth transition
      - Ambient lighting simulation (natural light through windows changes with day/night)
      - Configurable render quality (low/medium/high/ultra)
      - Grid overlay toggle
      - Axis helper display
      - Minimap showing top-down floor plan
      - Fullscreen viewport toggle
      - Screenshot/render capture from current camera position
    </3d_viewport_and_camera>

    <furniture_and_object_placement>
      - Drag items from library panel into the 3D scene
      - Free movement of objects (translate on X/Y/Z axes)
      - Rotation of objects (free rotate and snap-to-90-degree)
      - Scale objects (with optional lock to maintain proportions)
      - Lock dimensions for product-sourced furniture (maintain real-world size)
      - Snap-to-grid placement option (toggleable)
      - Snap-to-wall placement for wall-adjacent items (shelves, desks, etc.)
      - Measurement tool: measure distance between any two points
      - Dimensions visible on hover for any item or room
      - Multi-select objects (shift+click or box select)
      - Copy/paste objects within and across rooms
      - Undo/redo with full edit history panel
      - Edit history timeline showing all actions with ability to jump to any point
      - Object property inspector (position, rotation, scale, material, dimensions)
    </furniture_and_object_placement>

    <materials_and_customization>
      - Wall color picker (per individual wall)
      - Wall texture/wallpaper application (per individual wall)
      - Wall material presets (paint, brick, wood panel, tile, concrete, etc.)
      - Floor material selection per room (hardwood, tile, carpet, marble, laminate, concrete)
      - Floor color/pattern customization
      - Ceiling height adjustment per room
      - Ceiling material/color customization
      - Ceiling features (beams, skylights, recessed areas)
      - Place artificial lights (floor lamps, ceiling lights, wall sconces, table lamps)
      - Light properties: intensity, color temperature, cone angle
      - Natural window light simulation (affected by window placement and day/night cycle)
      - Day/night cycle affecting all lighting in scene
      - Material preview before applying
      - Custom texture upload for walls/floors
    </materials_and_customization>

    <ai_features>
      - Photo-to-room: upload apartment photo, AI estimates room layout and dimensions
      - Photo-to-room: AI generates wall positions, windows, doors from photo
      - Photo-to-furniture: upload furniture photo, generate 3D model via TRELLIS
      - AI dimension estimation from furniture photos
      - Manual dimension input/override for AI-generated models
      - Product URL import: paste URL, app scrapes product image and specifications
      - Product URL import: auto-extract dimensions from product specs
      - Product URL fallback: if scraping fails, prompt for different URL or manual entry with image
      - AI model generation retry with adjusted parameters if result is unsatisfactory
      - AI model generation progress indicator
      - Before/after comparison: generate "after" render matching camera angle of original photo
      - Before/after side-by-side or slider view
      - AI generation history (track all generated models)
      - API key configuration per AI service
      - TRELLIS integration for high-quality image-to-3D conversion
      - Graceful handling of AI service unavailability
    </ai_features>

    <asset_library>
      - Built-in starter library with essential furniture (multiple options per type):
        * Seating (sofas, armchairs, dining chairs, office chairs)
        * Tables (dining tables, coffee tables, side tables, desks)
        * Storage (bookshelves, wardrobes, dressers, cabinets)
        * Beds (single, double, queen, king with frames and mattresses)
        * Lighting (floor lamps, table lamps, ceiling lights, pendant lights)
        * Decor (rugs, curtains, picture frames, mirrors, clocks)
        * Plants (potted plants, hanging plants, large floor plants)
        * Windows (various styles and sizes)
        * Doors (interior doors, sliding doors)
      - Category-based browsing with icons
      - Search and filter assets by name, category, tags
      - Favorites system (star/unstar assets)
      - Custom tags and lists for organizing assets
      - User-generated assets section (AI-generated and imported models)
      - Clear expandability architecture with documentation for adding new catalogs
      - Instructions/guide for using LLMs to batch-add furniture from other catalogs
      - Asset detail view with dimensions, preview, and metadata
    </asset_library>

    <export_and_sharing>
      - High-quality screenshot/render from any camera angle
      - Export full 3D scene as glTF/GLB file
      - Export floor plan as PDF (2D top-down view with dimensions)
      - Export floor plan as PNG/SVG
      - Before/after image export (original photo + matched render)
      - Configurable export resolution and quality
      - Project export (full project backup as ZIP)
      - Project import (restore from ZIP backup)
    </export_and_sharing>

    <settings_and_configuration>
      - AI API key management (add, edit, remove keys for each service)
      - Unit system toggle (metric/imperial) affecting all displayed measurements
      - Default render quality setting
      - Auto-save interval configuration
      - Performance mode toggle (reduces visual quality for smoother editing on lower-end hardware)
      - Model compression/optimization settings (quality vs file size tradeoff)
      - Performance-based size limitation warnings (warn when project complexity may impact UX)
    </settings_and_configuration>

    <ui_shell_and_navigation>
      - Left sidebar: asset library browser with search, categories, favorites
      - Right sidebar: properties/inspector panel for selected objects
      - Top toolbar: tool selection (select, draw wall, place furniture, measure, etc.)
      - Floor switcher: vertical stack of floor thumbnails
      - Collapsible sidebars for maximum viewport space
      - Keyboard shortcuts for common operations
      - Context menus (right-click) on objects and surfaces
      - Sleek animations on panel open/close, tool switch, and mode changes
      - Empty state designs (new project, empty room, no results)
    </ui_shell_and_navigation>
  </core_features>

  <database_schema>
    <tables>
      <projects>
        - id (INTEGER PRIMARY KEY), name (TEXT), description (TEXT), thumbnail_path (TEXT)
        - unit_system (TEXT DEFAULT 'metric'), created_at (DATETIME), updated_at (DATETIME)
      </projects>
      <floors>
        - id (INTEGER PRIMARY KEY), project_id (INTEGER FK), level (INTEGER), name (TEXT)
        - order_index (INTEGER), created_at (DATETIME)
      </floors>
      <rooms>
        - id (INTEGER PRIMARY KEY), floor_id (INTEGER FK), name (TEXT)
        - dimensions_json (TEXT - wall vertices, area, shape data)
        - floor_material (TEXT), floor_color (TEXT), floor_texture_path (TEXT)
        - ceiling_height (REAL), ceiling_material (TEXT), ceiling_color (TEXT)
        - position_x (REAL), position_y (REAL), position_z (REAL)
        - created_at (DATETIME), updated_at (DATETIME)
      </rooms>
      <walls>
        - id (INTEGER PRIMARY KEY), room_id (INTEGER FK)
        - start_x (REAL), start_y (REAL), end_x (REAL), end_y (REAL), height (REAL)
        - material (TEXT), color (TEXT), texture_path (TEXT)
        - has_window (BOOLEAN), has_door (BOOLEAN)
      </walls>
      <assets>
        - id (INTEGER PRIMARY KEY), name (TEXT), category (TEXT), subcategory (TEXT)
        - source (TEXT - 'builtin'|'generated'|'imported'|'url_import')
        - model_path (TEXT), thumbnail_path (TEXT)
        - width (REAL), height (REAL), depth (REAL), dimension_locked (BOOLEAN)
        - source_url (TEXT), source_product_name (TEXT)
        - is_favorite (BOOLEAN DEFAULT FALSE)
        - created_at (DATETIME), updated_at (DATETIME)
      </assets>
      <asset_tags>
        - id (INTEGER PRIMARY KEY), asset_id (INTEGER FK), tag (TEXT)
      </asset_tags>
      <furniture_placements>
        - id (INTEGER PRIMARY KEY), room_id (INTEGER FK), asset_id (INTEGER FK)
        - position_x (REAL), position_y (REAL), position_z (REAL)
        - rotation_x (REAL), rotation_y (REAL), rotation_z (REAL)
        - scale_x (REAL DEFAULT 1), scale_y (REAL DEFAULT 1), scale_z (REAL DEFAULT 1)
        - locked (BOOLEAN DEFAULT FALSE)
        - created_at (DATETIME)
      </furniture_placements>
      <lights>
        - id (INTEGER PRIMARY KEY), room_id (INTEGER FK)
        - type (TEXT - 'floor_lamp'|'ceiling'|'wall_sconce'|'table_lamp'|'pendant')
        - position_x (REAL), position_y (REAL), position_z (REAL)
        - intensity (REAL), color (TEXT), cone_angle (REAL)
        - asset_id (INTEGER FK NULLABLE)
      </lights>
      <windows>
        - id (INTEGER PRIMARY KEY), wall_id (INTEGER FK)
        - position_along_wall (REAL), height_from_floor (REAL)
        - width (REAL), height (REAL), style (TEXT)
      </windows>
      <doors>
        - id (INTEGER PRIMARY KEY), wall_id (INTEGER FK)
        - position_along_wall (REAL), width (REAL), height (REAL), style (TEXT)
      </doors>
      <edit_history>
        - id (INTEGER PRIMARY KEY), project_id (INTEGER FK)
        - action_type (TEXT), action_data (TEXT JSON), timestamp (DATETIME)
        - snapshot_data (TEXT JSON - for undo/redo state restoration)
      </edit_history>
      <ai_generations>
        - id (INTEGER PRIMARY KEY), project_id (INTEGER FK NULLABLE)
        - type (TEXT - 'photo_to_room'|'photo_to_furniture'|'url_import')
        - input_image_path (TEXT), input_url (TEXT)
        - output_model_path (TEXT), output_dimensions (TEXT JSON)
        - status (TEXT - 'pending'|'processing'|'completed'|'failed')
        - error_message (TEXT), created_at (DATETIME)
      </ai_generations>
      <user_settings>
        - key (TEXT PRIMARY KEY), value (TEXT), encrypted (BOOLEAN DEFAULT FALSE)
      </user_settings>
      <material_presets>
        - id (INTEGER PRIMARY KEY), name (TEXT), type (TEXT - 'wall'|'floor'|'ceiling')
        - color (TEXT), texture_path (TEXT), properties_json (TEXT)
        - is_builtin (BOOLEAN DEFAULT TRUE)
      </material_presets>
    </tables>
  </database_schema>

  <api_endpoints_summary>
    <projects>
      - GET    /api/projects (list all projects)
      - POST   /api/projects (create new project)
      - GET    /api/projects/:id (get project details with floors/rooms)
      - PUT    /api/projects/:id (update project)
      - DELETE /api/projects/:id (delete project)
      - POST   /api/projects/:id/duplicate (duplicate project)
      - POST   /api/projects/:id/export (export as ZIP)
      - POST   /api/projects/import (import from ZIP)
    </projects>
    <floors>
      - GET    /api/projects/:projectId/floors (list floors)
      - POST   /api/projects/:projectId/floors (create floor)
      - PUT    /api/floors/:id (update floor)
      - DELETE /api/floors/:id (delete floor)
      - PUT    /api/floors/reorder (reorder floors)
    </floors>
    <rooms>
      - GET    /api/floors/:floorId/rooms (list rooms on floor)
      - POST   /api/floors/:floorId/rooms (create room)
      - PUT    /api/rooms/:id (update room - dimensions, materials, etc.)
      - DELETE /api/rooms/:id (delete room)
      - GET    /api/rooms/:id/furniture (list furniture in room)
    </rooms>
    <walls>
      - PUT    /api/walls/:id (update wall material/color/texture)
      - POST   /api/rooms/:roomId/walls (add wall segment)
      - DELETE /api/walls/:id (remove wall)
    </walls>
    <furniture>
      - POST   /api/rooms/:roomId/furniture (place furniture)
      - PUT    /api/furniture/:id (update position/rotation/scale)
      - DELETE /api/furniture/:id (remove furniture)
      - POST   /api/furniture/batch (batch update positions)
    </furniture>
    <lights>
      - POST   /api/rooms/:roomId/lights (place light)
      - PUT    /api/lights/:id (update light properties)
      - DELETE /api/lights/:id (remove light)
    </lights>
    <assets>
      - GET    /api/assets (list with search, filter, category, tags)
      - GET    /api/assets/:id (get asset details)
      - POST   /api/assets (upload/create custom asset)
      - PUT    /api/assets/:id (update asset metadata)
      - DELETE /api/assets/:id (delete custom asset)
      - PUT    /api/assets/:id/favorite (toggle favorite)
      - POST   /api/assets/:id/tags (add tags)
      - DELETE /api/assets/:id/tags/:tag (remove tag)
    </assets>
    <ai>
      - POST   /api/ai/photo-to-room (upload photo, get room reconstruction)
      - POST   /api/ai/photo-to-furniture (upload photo, generate 3D model via TRELLIS)
      - POST   /api/ai/url-import (scrape product URL for image + specs)
      - POST   /api/ai/before-after (generate matched render from original photo angle)
      - GET    /api/ai/generations (list AI generation history)
      - GET    /api/ai/generations/:id (get generation status/result)
      - POST   /api/ai/generations/:id/retry (retry failed generation)
    </ai>
    <export>
      - POST   /api/export/screenshot (render screenshot from camera position)
      - POST   /api/export/scene (export 3D scene as glTF/GLB)
      - POST   /api/export/floorplan (export floor plan as PDF/PNG/SVG)
    </export>
    <settings>
      - GET    /api/settings (get all settings)
      - PUT    /api/settings (update settings)
      - POST   /api/settings/api-keys/validate (test API key validity)
    </settings>
    <edit_history>
      - GET    /api/projects/:projectId/history (get edit history)
      - POST   /api/projects/:projectId/undo (undo last action)
      - POST   /api/projects/:projectId/redo (redo last undone action)
    </edit_history>
  </api_endpoints_summary>

  <ui_layout>
    <main_structure>
      The app has two main views: the Project Hub (landing page) and the Editor.

      PROJECT HUB:
      - Clean grid/list of projects with thumbnail previews
      - Create new project button (prominent)
      - Search/filter projects
      - Quick actions: open, duplicate, delete, export
      - Settings access from header
      - Sleek animations on hover and transitions

      EDITOR (main workspace):
      - Top Bar: Tool selection (pointer/select, draw wall, place item, measure, pan, first-person),
        undo/redo buttons, save indicator, project name, settings gear, back to hub
      - Left Sidebar (collapsible, ~280px):
        * Asset library with category tabs (Furniture, Decor, Plants, Lighting, Windows/Doors)
        * Search bar at top
        * Grid view of assets with thumbnails
        * "Generate from Photo" and "Import from URL" buttons at top
        * Favorites and custom lists toggle
      - Center: Full 3D viewport (Three.js canvas)
        * Grid overlay (toggleable)
        * Minimap in corner (top-down view)
        * Camera mode indicator
        * Day/night toggle floating button
      - Right Sidebar (collapsible, ~300px):
        * Properties panel for selected object (transform, material, dimensions)
        * Room properties when room selected
        * Material picker with presets
        * No selection: shows project/floor overview
      - Bottom (collapsible):
        * Edit history timeline (horizontal scrollable)
      - Floor Switcher (right edge, vertical):
        * Small thumbnail per floor
        * Current floor highlighted
        * + button to add floor
        * Drag to reorder
    </main_structure>
    <modals_and_overlays>
      - AI Generation modal: shows progress, preview, retry options
      - Product URL import dialog: URL input, preview of scraped data, dimension editor
      - Export dialog: format selection, quality settings, preview
      - Before/After comparison: slider overlay or side-by-side modal
      - Settings modal/page: tabbed (General, AI Keys, Performance, About)
      - Keyboard shortcuts reference overlay
      - Confirmation dialogs for destructive actions (styled, not browser default)
    </modals_and_overlays>
  </ui_layout>

  <design_system>
    <color_palette>
      - Background: Deep charcoal/near-black (#0A0A0F) for editor, subtle gray (#F8F9FA) for hub
      - Surface: Dark surfaces (#16161D, #1E1E28) for panels and sidebars
      - Primary accent: Electric blue (#3B82F6) for interactive elements and selections
      - Secondary accent: Soft violet (#8B5CF6) for AI features and generation indicators
      - Success: Emerald green (#10B981)
      - Warning: Amber (#F59E0B)
      - Error: Rose (#EF4444)
      - Text primary: White (#FAFAFA) on dark, near-black (#0A0A0F) on light
      - Text secondary: Muted gray (#9CA3AF)
      - Borders: Subtle (#2A2A35) on dark surfaces
      - Hover/Active states: Lighten surface by 8-12%
      - The editor uses a dark theme by default (better for 3D visualization)
      - The project hub can use light or dark theme
    </color_palette>
    <typography>
      - Primary font: Inter (clean, modern, excellent readability)
      - Monospace: JetBrains Mono (for dimensions, measurements, coordinates)
      - Headings: Inter Semi-Bold, tracking -0.02em
      - Body: Inter Regular, 14px base
      - Small/Labels: Inter Medium, 12px
      - Dimensions display: JetBrains Mono, 13px
    </typography>
    <design_principles>
      - Dark editor aesthetic (common in 3D/design tools - Blender, Figma)
      - Generous whitespace in panels
      - Subtle glassmorphism on floating elements (minimap, day/night toggle)
      - Smooth 200-300ms transitions on all interactive elements
      - Micro-animations: panel slide, tool switch pulse, selection glow
      - Consistent 8px spacing grid
      - Rounded corners (8px default, 12px for cards, 16px for modals)
      - Subtle shadows and depth hierarchy
      - Tooltips on all toolbar buttons and non-obvious interactive elements
      - Loading skeletons instead of spinners where possible
      - Inspired by 21st.dev component aesthetics
    </design_principles>
  </design_system>

  <implementation_steps>
    <step number="1">
      <title>Foundation and Project Management</title>
      <tasks>
        - Set up React + Vite + TypeScript project structure
        - Configure Tailwind CSS + shadcn/ui
        - Set up Express backend with SQLite database
        - Implement database schema and migrations
        - Build Project Hub UI (list, create, delete, duplicate projects)
        - Implement project CRUD API endpoints
        - Set up basic routing between Hub and Editor
      </tasks>
    </step>
    <step number="2">
      <title>3D Viewport and Room Editor Core</title>
      <tasks>
        - Integrate React Three Fiber with editor layout
        - Implement isometric camera with orbit/pan/zoom controls
        - Build wall drawing tool (Sims-style rectangle drag with live dimensions)
        - Implement room creation from dimension input
        - Room resizing by dragging walls/corners
        - Room attachment (connecting rooms to form apartment)
        - Grid system and snap-to-grid
        - Basic room rendering with floor/wall/ceiling
      </tasks>
    </step>
    <step number="3">
      <title>Editor UI Shell</title>
      <tasks>
        - Build left sidebar (asset library panel) with categories
        - Build right sidebar (properties/inspector panel)
        - Build top toolbar with tool selection
        - Build floor switcher component
        - Implement collapsible panels
        - Add keyboard shortcuts
        - Build edit history timeline (bottom panel)
        - Context menus on right-click
      </tasks>
    </step>
    <step number="4">
      <title>Furniture Placement and Manipulation</title>
      <tasks>
        - Drag-and-drop from library to 3D scene
        - Object selection, translation, rotation, scaling gizmos
        - Snap-to-wall for wall-adjacent items
        - Dimension display on hover
        - Measurement tool between points
        - Multi-select and batch operations
        - Copy/paste objects
        - Implement undo/redo system with history tracking
      </tasks>
    </step>
    <step number="5">
      <title>Materials, Lighting, and Customization</title>
      <tasks>
        - Material system for walls (per-wall color, texture, wallpaper)
        - Floor material system (per-room)
        - Ceiling customization (height, material, features)
        - Light placement and property editing
        - Natural light through windows
        - Day/night cycle toggle
        - Material presets library
        - Custom texture upload
      </tasks>
    </step>
    <step number="6">
      <title>Multi-Floor Support</title>
      <tasks>
        - Multi-floor data model and API
        - Floor switcher with thumbnails
        - Opacity rendering (current floor full, others translucent)
        - Add/remove/reorder floors
        - Staircase/opening placement between floors
        - Camera position memory per floor
        - Smooth floor transition animations
      </tasks>
    </step>
    <step number="7">
      <title>Asset Library System</title>
      <tasks>
        - Built-in starter library with essential furniture models
        - Category browsing with icons and subcategories
        - Search and filter functionality
        - Favorites system
        - Custom tags and lists
        - Asset detail view with metadata
        - Expandability documentation and LLM instructions for adding catalogs
        - User-generated assets section
      </tasks>
    </step>
    <step number="8">
      <title>AI Integration</title>
      <tasks>
        - TRELLIS integration for photo-to-3D model generation
        - Photo-to-room AI reconstruction
        - Floor plan upload and conversion
        - Product URL scraping (image + specs extraction)
        - AI dimension estimation
        - Generation progress UI and retry flow
        - AI generation history tracking
        - Before/after comparison with camera angle matching
      </tasks>
    </step>
    <step number="9">
      <title>Export and Persistence</title>
      <tasks>
        - Screenshot/render capture system
        - 3D scene export (glTF/GLB)
        - Floor plan export (PDF, PNG, SVG)
        - Project export/import (ZIP backup)
        - Before/after image export
        - Auto-save implementation
        - Model compression and optimization
        - Performance monitoring and adaptive quality
      </tasks>
    </step>
    <step number="10">
      <title>Settings, Polish, and Performance</title>
      <tasks>
        - Settings page (API keys, units, render quality, performance)
        - Metric/imperial unit toggle throughout app
        - First-person walkthrough camera mode
        - UI animations and transitions polish
        - Performance optimization (LOD, model compression, lazy loading)
        - Performance-based project size warnings
        - Empty states for all views
        - Error handling and user feedback throughout
        - Keyboard shortcuts reference
        - Future cloud-readiness: clean data layer abstraction
      </tasks>
    </step>
  </implementation_steps>

  <success_criteria>
    <functionality>
      - All room creation methods work reliably (draw, dimensions, floor plan, AI)
      - Furniture placement, manipulation, and snapping feels natural and precise
      - 3D viewport is responsive with smooth camera controls
      - AI features (TRELLIS photo-to-3D, photo-to-room) produce usable results
      - Product URL import successfully scrapes supported sites with graceful fallback
      - Multi-floor editing works seamlessly with intuitive switching
      - All export formats produce high-quality output
      - Projects save and load reliably with no data loss
    </functionality>
    <user_experience>
      - Editor feels reactive and performant - no laggy interactions
      - Room drawing and resizing feels natural (no glitchy rooms, smooth dragging)
      - Furniture placement is intuitive with helpful snapping
      - UI animations are smooth and purposeful (not distracting)
      - Clear visual feedback for all user actions
      - Empty states guide the user on what to do next
      - Keyboard shortcuts accelerate workflow
      - Error messages are helpful and actionable
    </user_experience>
    <technical_quality>
      - App loads and renders within 3 seconds on modern hardware
      - Smooth 60fps in the 3D viewport during normal editing
      - No memory leaks during extended editing sessions
      - Graceful degradation on lower-end hardware (performance mode)
      - Clean REST API with proper error handling
      - Data integrity maintained across save/load cycles
      - 3D model optimization keeps file sizes reasonable
    </technical_quality>
    <design_polish>
      - Dark editor theme looks professional and modern (Blender/Figma tier)
      - Consistent visual language across all UI elements
      - Animations feel polished and intentional
      - Typography is crisp and readable at all sizes
      - Color system creates clear visual hierarchy
      - UI matches 21st.dev aesthetic quality
      - No visual glitches, overflow issues, or broken layouts
    </design_polish>
  </success_criteria>

  <expandability_notes>
    <library_expansion>
      To add new furniture catalogs beyond the built-in IKEA essentials:
      1. Prepare 3D models in glTF/GLB format with proper dimensions
      2. Add entries to the assets table with category, dimensions, and model paths
      3. Place model files in the assets storage directory
      4. Tag appropriately for search/filter discoverability

      For LLM-assisted batch expansion:
      - Provide the LLM with the asset database schema and API endpoints
      - Use the POST /api/assets endpoint to add new items
      - Include: name, category, subcategory, dimensions (width/height/depth), model_path, thumbnail
      - Models can be sourced from open 3D model libraries (Sketchfab, TurboSquid free, etc.)
      - Instruct the LLM to maintain consistent naming conventions and categorization
    </library_expansion>
    <cloud_readiness>
      The data layer is designed with future cloud migration in mind:
      - All data access goes through the REST API (not direct DB access from frontend)
      - File storage paths are abstracted (can swap local filesystem for S3/cloud storage)
      - SQLite can be migrated to PostgreSQL with minimal schema changes
      - Project export/import provides a portable backup format
    </cloud_readiness>
  </expandability_notes>
</project_specification>
